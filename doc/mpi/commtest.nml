
#
# $Id$
#

# input sizes for arrays, number of transposes, etc

# real values would be more like:
   #ensemble_size = 20,
   #state_vector_size = 1000000,
   #observation_count = 1000 /

# test/debug values are smaller.

&commtest_nml
   ensemble_size = 8,
   state_vector_size = 120,
   observation_count = 24,
   repeat_count = 10,
   select_algorithm = 1,
   state_complete_algorithm = 3,
   broadcast_algorithm = 3,
   ensemble_complete_algorithm = 3 /


