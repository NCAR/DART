You need to have the rttov coeffient files for rttov_test

I'm not sure what rttov_test tests

the quickbuild.csh in work is the quickbuild.csh from the utilities directory


./preprocess; make; ./rttov_unit_tests


git push helen feature/rttov-tests
